---
interface Props {
  title: string;
  subtitle: string;
}
const { title, subtitle } = Astro.props;
---

<div class='my-16 text-center'>
  <h1 class='text-7xl font-black mb-4'>{title}</h1>
  <h4 class='h4 font-semibold italic'>{subtitle}</h4>
</div>
